# CASH Build System — v1.2 Iteration Plan

**Date:** 2026-03-10
**Current Version:** 1.1-beta
**Source:** Code review + edge-case testing against official hooks reference docs

---

## Issues Identified

### Fix Now (v1.1-beta patch)

| # | Severity | Issue | Fix |
|---|----------|-------|-----|
| 1 | MEDIUM | `jq` dependency not validated — both scripts silently fail open if `jq` missing, disabling all enforcement without warning | Add `jq` check to Step 9 validation + guard at top of each script |
| 2 | LOW | JSON injection via `$BASENAME` in `check-sequential-files.sh` — filenames with `"` or `\` produce malformed JSON output | Replace heredoc with `jq -n --arg` to safely build JSON |
| 3 | LOW | Comment says "untracked" but code excludes untracked files via `grep -vE '^\?\?'` in `stop-check.sh` | Update comment to match code |
| 4 | LOW | Stop hook fires unhelpful message when TRACES.md doesn't exist — says "Add iteration entry" but there's no file | Check TRACES.md existence, adjust message to "Create TRACES.md" |

### Defer to v1.2

| # | Type | Issue | Notes |
|---|------|-------|-------|
| 5 | Design | 1-hour window blind spot — code changes within 1hr of TRACES.md update aren't caught | Could compare TRACES.md mod time against newest code file mod time instead of `$NOW`. Needs design thought — adds complexity |
| 6 | Design | `sed '/^---$/q'` fragile to user edits — a `---` inside Project Summary truncates compact hook output | Consider sentinel comment (`<!-- end-header -->`) approach. Low risk currently |
| 7 | Design | PostToolUseFailure prompt timing mismatch — asks about "success with different approach" at the moment of failure | Rephrase to future tense: "When you find a working approach, log the pattern." Minor wording change |
| 8 | INFO | v1.1 plan incorrectly stated `agent_type` doesn't exist — it does per official docs, but `agent_id` is still the better choice | Correct the claim in version history or plan for accuracy |

---

## Execution

### Patch (applying now)
1. Update `check-sequential-files.sh` template: add jq guard, replace heredoc with `jq -n --arg`
2. Update `stop-check.sh` template: add jq guard, fix comment, add TRACES.md existence check
3. Update Step 9 validation: add `jq` check
4. Update version history with patch note

### v1.2 (future iteration)
- Items 5-8 above
- Any issues found in real-project testing
